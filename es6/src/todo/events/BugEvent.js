import AbstractBugEvent from "../../../gen/todo/events/AbstractBugEvent";

export default class BugEvent extends AbstractBugEvent {
    prepareDataForView() {
        this.eventData = JSON.parse(JSON.stringify(this.eventParam));
        if (this.eventData.data === undefined) {
        	this.eventData.data = {};
        }
    }
}

/*       S.D.G.       */
