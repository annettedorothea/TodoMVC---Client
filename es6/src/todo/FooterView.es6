'use strict';

class FooterView {
    static initFilter(eventData) {
        container.setState({
            filter: eventData.filter
        });
    };
    
}

/*                    S.D.G.                    */
