{
  "name": "todo",
  "version": "0.5.1",
  "description": "",
  "scripts": {
    "server": "watchify ./es6/src/app/App.js --standalone Todo -o lib/todo.0.5.1.js -dv & live-server --watch='lib/todo.0.5.1.js' --host=127.0.0.1 --port=9999 --proxy='/api/':'http://localhost:8096/api/' --proxy='/replay/':'http://localhost:8098/replay/' --browser='Google Chrome'",
    "dev-win": "start npm run watch && start npm run start-server",
    "watch": "watchify ./es6/src/app/App.js --standalone Todo -o lib/todo.0.5.1.js -dv",
    "build": "browserify ./es6/src/app/App.js --standalone Todo -o lib/todo.0.5.1.js",
    "start-server": "live-server --watch=lib/todo.0.5.1.js --host=127.0.0.1 --port=9999 --proxy=/api/:http://127.0.0.1:8096/api/ --proxy=/replay/:http://127.0.0.1:8098/replay/"
  },
  "author": "Annette Pohl",
  "license": "ISC",
  "browserify": {
    "transform": [
      [
        "babelify",
        {
          "presets": [
            "es2015",
            "react"
          ]
        }
      ]
    ]
  },
  "dependencies": {
    "json-stable-stringify": "^1.0.1",
    "react": "^16.8.2",
    "react-dom": "^16.8.2"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-preset-react": "^6.24.1",
    "babelify": "^8.0.0",
    "browserify": "^14.5.0",
    "live-server": "^1.2.1",
    "watch": "^1.0.2",
    "watchify": "^3.11.0"
  }
}
